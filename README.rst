==========
geom-check
==========

Check different methods for rounding digits inside a geometry.

Run
===

::

   python3 -m venv --clear venv
   ./venv/bin/pip install -r requirements.txt
   ./venv/bin/python test_rounding.py

